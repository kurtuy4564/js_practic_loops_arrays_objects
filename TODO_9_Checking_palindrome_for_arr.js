isPalindrome = chars => {
  return
}

// Вход:
const chars = ['т', 'о', 'п', 'о', 'т']

// Вызов:
const result = isPalindrome(chars)
console.log(result)

// Ожидаемый результат: true

// Условие: Напиши isPalindrome(chars), которая проверит, является ли массив символов палиндромом
// (одинаково читается слева направо и справа налево).
